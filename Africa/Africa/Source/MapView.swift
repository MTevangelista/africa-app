import SwiftUI

struct MapView: View {
    var body: some View {
        Text("map")
    }
}

struct MapView_Previews: PreviewProvider {
    static var previews: some View {
        MapView()
    }
}
